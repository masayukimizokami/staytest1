<h1>予約フォーム</h1>

<ol class="nav">
	<li>1.入力</li>
	<li class="current">2.確認</li>
	<li>3.完了</li>
</ol>

<dl>
	<dt>希望日</dt>
	<dd><%= @reservation.reserve_date %></dd>
	<dt>名前</dt>
	<dd><%= @reservation.name %></dd>
	<dt>備考</dt>
	<dd><%= @reservation.note %></dd>
</dl>

<%= form_for @reservation, method: :post, url: reservation_back_path do |form| %>
	<%= form.hidden_field :reserve_date %>
	<%= form.hidden_field :name %>
	<%= form.hidden_field :note %>
	<%= form.submit '入力画面に戻る' %>
<% end %>

<%= form_for @reservation, method: :post, url: reservation_complete_path do |form| %>
	<%= form.hidden_field :reserve_date %>
	<%= form.hidden_field :name %>
	<%= form.hidden_field :note %>
	<%= form.submit '送信する' %>
<% end %>